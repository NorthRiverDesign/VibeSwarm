@* 11px uppercase tracking-wide label used above groupings. *@

<div class="text-eyebrow @CssClass">@ChildContent</div>

@code {
	[Parameter] public RenderFragment? ChildContent { get; set; }
	[Parameter] public string? CssClass { get; set; }
}
