# VibeSwarm Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit the .env file to version control!

# Admin User Credentials
# These are used to create the initial admin user on first startup
DEFAULT_ADMIN_USER=admin
DEFAULT_ADMIN_PASS=YourSecurePassword123!

# Note: If DEFAULT_ADMIN_PASS is not set, a random password will be generated
# and displayed in the console logs on first startup (one time only).
